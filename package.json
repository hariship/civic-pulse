{
  "name": "civic-pulse",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd backend && source venv/bin/activate && uvicorn main:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "build:all": "cd frontend && npm install && npm run build && cd ../backend && pip install -r requirements.txt",
    "build:export": "cd frontend && npm run build && npm run export",
    "start": "concurrently \"cd backend && source venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000\" \"cd frontend && npm start\"",
    "start:prod": "concurrently \"cd backend && source venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000\" \"cd frontend && npm run build && npm start\"",
    "deploy": "cd /home/ubuntu/civic-pulse && git pull origin main && docker-compose up -d --build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}